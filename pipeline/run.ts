/**
 * Data pipeline entry point — built in Phase 1 (see ROADMAP.md §2.3).
 * Steps: fetch universe → index constituents → enrich profiles → FX snapshot
 * → derive fields → validate (Zod) → emit artifacts to /public/data.
 */
console.log("Pipeline not implemented yet — Phase 1. See ROADMAP.md §2.3.");
